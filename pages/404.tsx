import { NextPage } from "next"

const NotFoundPage: NextPage = () => {
    // console.log(query?.myId);

    return (
        <div>
            not found page gamw to spiti!
        </div>
    )
}

export default NotFoundPage